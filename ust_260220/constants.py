"""Shared constants for G1 Kitchen Sorting project."""

# 테이블 물리적 높이 (Cuboid size.z)
TABLE_HEIGHT = 1.0  # meters

# 테이블 상단 표면 z좌표 (pos.z = H/2, size.z = H → surface = H/2 + H/2 = H)
TABLE_SURFACE_Z = TABLE_HEIGHT
